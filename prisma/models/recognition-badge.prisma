model Badge {
  id           String        @id @default(uuid())
  title        String
  category     BadgeCategory
  iconImage    String
  recognitions Recognition[]
}

enum BadgeCategory {
  MILESTONE
  GOOD_JOB
  ANNIVERSARY
  PROMOTION
}
