import { Injectable } from "@nestjs/common";

@Injectable()
export class BuillMqService {
    // constructor(@InjectQueue("default") private readonly queue: Queue) {}
    // async addJob<D = any>(name: string, data: D) {
    //   await this.queue.add(name, data);
    // }
}
