name: "Setup Node.js and pnpm"
description: "Sets up Node.js and pnpm for the project"
inputs:
    node-version:
        description: "Node.js version"
        required: true
        default: "22"
runs:
    using: "composite"
    steps:
        - name: 🟢 Setup Node.js
          uses: actions/setup-node@v4
          with:
              node-version: ${{ inputs.node-version }}
        - name: Setup pnpm
          uses: pnpm/action-setup@v2
          with:
              version: 10
        - name: Install dependencies
          shell: bash
          run: pnpm install
